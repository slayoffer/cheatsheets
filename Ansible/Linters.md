### Linters

```bash
- YAML lint # best
- Ansible Lint # medium
- ansible-playbook \ --syntax-check # light check
```

### Ansible lint

```bash
# install
sudo apt install ansible-lint

# run
ansible-lint playbook.yml
```

