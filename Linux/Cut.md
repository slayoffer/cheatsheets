### Basics

```bash
cut —d';'-f1,4 fi1e
# Cut out selected portions of each line of a file. 
# The —d option defines the field delimiter character (here ;) instead of the default tab character. 
# The —f option specifics the field(s) displayed (here the first and the fourth).
```

